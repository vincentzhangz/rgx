# 2026-08-15 — rgx 0.1.0 (`532d51c`)

Protocol: [../BENCHMARKS.md](../BENCHMARKS.md).

Host: Windows, 24 logical CPUs, rustc 1.97.1, ripgrep 15.2.0.
Corpus: 8,000 synthetic files. Search stdout discarded. Median of 5 query
runs after 1 warmup (builds: 3 runs).

## Prune

| Pattern | Candidates | Matches |
|---------|----------:|--------:|
| `sits here` | 1 | 1 |
| `needle_token_UNIQUE_8000` | **8,000** | 1 |

Mixed-case literals did not prune. Query plans folded extracted literals;
`file_grams` hashed raw bytes.

## Time and memory (`sits here`, pruned)

| | rgx 0.1.0 | ripgrep 15.2.0 |
|--|----------:|---------------:|
| Wall | 64 ms | 100 ms |
| CPU (user+kernel) | 63 ms | 750 ms |
| Working set | 101 MiB | 15 MiB |
| Private bytes | 3.2 MiB | 10 MiB |

`Index::open` was 32–44 ms and faulted the whole posting table into the
working set.

Index on disk was about **8.7×** the corpus.
